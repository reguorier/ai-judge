# AI Judge — P8 Release Freeze Record

## Freeze Metadata

| Field | Value |
|---|---|
| **Freeze Version** | P8 |
| **Freeze Date** | 2026-06-03 |
| **Frozen By** | P8 Release Freeze & Regression Harness |
| **Schema Version** | ai-judge-release-readiness-v1 |
| **Build ID** | p8.2-release-shortcut-e2e-v1 |
| **Sample Run ID** | 1142374c8b6d |

## Scope of Freeze (P0–P7.3)

| Phase | Feature | Status |
|---|---|---|
| P0 | Judge Core — verdict, seats, trace, output rendering | Frozen |
| P1 | Hermes Output — JSON/MD, index | Frozen |
| P2 | Gavel (Human) — status, sync, history, digest | Frozen |
| P3 | Claim Calibration — per-run, index, rebuild | Frozen |
| P4 | Run Universe — listing, summary, stop/resume | Frozen |
| P5 | Trust Calibration — per-seat scoring | Frozen |
| P6 | Citation Dashboard & Pipeline MVP | Frozen |
| P7.1 | Rescue / Supplement / Recheck | Frozen |
| P7.2 | DI Shortcut & E2E v1 (BUILD_ID marker) | Frozen |
| P7.3 | Decision Intelligence API | Frozen |

## Regression Test Summary

### Core Artifacts
- 12/12 Run files present
- 4/4 Global files present
- 5/5 Vault Indexes present

### Core APIs (15 endpoints)
All 15 endpoints returning HTTP 200:
- `/api/health`, `/api/judge/<id>/verdict`, `/api/runs/<id>/index.html`
- `/api/runs/<id>/hermes-output.json`, `/api/hermes/index`, `/api/hermes/seats`
- `/api/gavel/<id>`, `/api/gavel/<id>/history`, `/api/gavel/digest`
- `/api/claims/calibration`, `/api/claims/calibration/<id>`
- `/api/runs/universe`, `/api/trust/calibration`, `/api/trust/seat/gemini`
- `/api/decision/intelligence`

### Dashboard Build
BUILD_ID `p8.2-release-shortcut-e2e-v1` verified in dashboard.js.

### Trace Smoke
POST /api/trace → 200, event confirmed in debug-ui-io-trace.jsonl.

### Sample Run
Verdict API for `1142374c8b6d` returns valid judgment.

## Known Limitations

1. **No automated UI/E2E tests**: Regression harness covers API + file artifacts only; browser-level interaction (dashboard.js rendering, werewolf boards, citation UI) is not validated.
2. **No performance benchmarks**: Response time / throughput not measured in this freeze.
3. **No cross-platform validation**: macOS-only. Windows/Linux paths not tested.
4. **Single sample run**: Only one run (1142374c8b6d) used as representative; extreme edge-case runs not exhaustively sampled.
5. **Vault warnings are non-blocking**: Missing vault index files are treated as warnings, not blockers (they can be regenerated).

## Unfreeze Conditions

The P8 freeze may be lifted when **any** of the following occur:

1. A critical bug is discovered in P0–P7.3 scope requiring a hotfix (→ P8.1 patch).
2. A new P9 feature is ready for integration and passes the expanded regression harness.
3. The dashboard BUILD_ID is incremented (e.g., p8.x-*).
4. A schema migration is required that changes the format of any output file.

## Files Produced by This Freeze

- `product/regression_harness.py` — One-click regression test runner
- `product/release_readiness.py` — Readiness report generator (JSON + MD)
- `runs/release-readiness.json` — Machine-readable readiness report
- `Vault/Indexes/release-readiness.md` — Human-readable readiness report
- `product/RELEASE_FREEZE_P8.md` — This document

---

*Generated by P8 Release Freeze & Regression Harness — 2026-06-03*

---

## P8.3 Baseline Refresh

- **Previous build**: p7.2-di-shortcut-e2e-v1
- **Current release build**: p8.2-release-shortcut-e2e-v1
- **Reason**: P8.2 added Release Status keyboard E2E path (Cmd+Shift+R/M).
- **Expected regression status**: pass

## P8.11 Post-Quarantine Baseline Refresh
- **Date**: 2026-06-03
- **Build ID**: p8.7-drift-sentinel-e2e-v1
- **Reason**: P8.7 drift sentinel + P8.9 auto-quarantine (35 files) + P8.10 manual review quarantine (24 files) completed
- **Unexpected drift**: []
- **Decision**: refresh_baseline
- **Action**: regression_harness BUILD_ID updated, freeze manifest regenerated with new baseline, readiness re-run
